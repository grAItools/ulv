# Path-scoped Claude Code rules go here. Each file is a markdown fragment
# with optional `paths:` frontmatter; Claude Code loads files in this
# directory whose `paths:` glob matches files being edited.
